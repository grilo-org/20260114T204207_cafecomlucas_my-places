import { css } from '@emotion/react'

const styles = css`
  width: 100%;
  height: 100%;

  &.leaflet-container {
    background: var(--background);
    a {
      font-size: 0.75rem;
    }
  }
`

export default styles
